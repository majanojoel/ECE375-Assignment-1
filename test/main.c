#include "wut.h"

#include <stdio.h>

void check(int x) {
    printf("Check: %d\n", x);
}

int main() {
    /*
    You may write any of your own testing code here.

    You can execute it using `build/test/wut`.

    Place at least one check that you'd like to turn into an assert in a test
    case. It should also help your implementation by telling you the result.
    */
    return 0;
}
